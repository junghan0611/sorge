# TRIAGE — 은퇴했다. 판은 커맨드로 본다

```bash
./run.sh board          # 라이브 이슈판 (gh + 대장 join, ~1.6s)
./run.sh board --debt   # 미분류만 = 빚 전부
```

**이 파일을 읽지 마라. 여기엔 아무 상태도 없다.** 습관으로 열었다면 위 커맨드 하나면 된다.

## 왜 비었나

상태는 **이슈 라벨**에 산다. 문서가 아니라 판정 대상 위에 붙어 있어서 갈라질 수 없다.

`ns:value` 세 축뿐이다 (문법은 `forge-config` 스위퍼 규약에서 물려받았다 — 어휘는 다르다):

| 축 | 값 | 뜻 |
|---|---|---|
| `house:<repo>` | 대장의 집 이름 | 누구 몫인가. 여럿 가능. **이슈 리포와 일하는 집이 다를 때만** 붙인다 |
| `state:` | `ready` `running` `review` `proposed` `blocked` `parked` | 생애 한 칸. **단일값 — `label-set`(기존 값 제거 + 새 값 하나)** |
| `ball:` | `owner` `glg` `sorge` | 다음에 누가 움직이나. **단일값 — 같은 규칙** |

둘 이상 붙으면 `board` 가 `⚠ 단일값 위반` 로 띄운다. API 반환순 첫 값을 조용히 읽지 않는다.

**라벨 없음 = 미분류이고, 그게 빚의 전부다.** 적어둘 필요가 없으니 낡을 수도 없다.

`done_when` · `impl=` · `defects=` 는 문자열이라 라벨이 못 든다 —
**attempt 워크트리**(`~/repos/wt/<house>/<이슈>/NEXT--attempt-*`)에 살고 **그 집 시민이 쓴다.**
sorge 는 거기에 쓰지 않고 남의 이슈에 코멘트도 달지 않는다(`LOOP.md § never` — 라벨만 열렸다).

`wt=` 는 아예 저장하지 않는다. 그 경로는 판정이 아니라 **규약**이라 유도된다 —
저장하면 사라진 워크트리와 짝이 맞는 낡은 문자열이 남고, 그게 `done_when` 둘째 게이트가
없는 디렉터리를 향해 초록불을 켤 수 있었던 경로다. `board` 가 매번 실재를 확인하고,
`state:running|review|proposed` 인데 워크트리가 없으면 경고로 띄운다.

## 이 파일이 죽은 이유 (2026-09-10)

83줄짜리 표가 나흘에 두 번 낡았다. 대장이 넓어질 때마다 「보류(대상 아님)」줄이 전부 거짓이 됐는데
**바이트는 하나도 안 바뀌었다.** `LOOP.md § done_when` 의 첫 게이트는 「적혀 있나」만 묻고
「적힌 게 아직 참인가」는 못 물어서, **측정이 초록불인 채로 답이 틀려 있었다**(09-08 세 집 · 09-09 `andenken`).

파일은 두 번째 질문에 답할 수 없다. 커맨드는 답한다 — 기억하지 않고 매번 다시 유도하기 때문이다.

> GLG, 2026-09-10: *"보고 적고 하면 끝도 없다. … 그렇게 되야 문서에 적느라 삽질을 안해 문서는 금방 썩어."*

옛 83줄과 tick log 전문은 `git log -- TRIAGE.md` 에 그대로 있다. 되살릴 것이 아니라 감사 자료다.
