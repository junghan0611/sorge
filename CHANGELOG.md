# CHANGELOG — sorge

닫힌 것만 여기 산다. 앞으로의 축은 `NEXT.md` 가 든다.

## Unreleased

## v2026.9.10 — 상태가 문서를 떠났다

`v2026.9.8` 이 「이 집이 무엇이 아닌지」를 알아낸 나흘이었다면, 이번은 **그 앎이 물건이 된 하루**다.
문서가 들고 있던 상태를 이슈 라벨로 내보내고, 문서 자신을 줄였다.

### 상태면 — 문서에서 이슈로

- **`TRIAGE.md` 은퇴. 56,928 → 554 bytes.** 83줄 표가 나흘에 두 번 낡았는데 **바이트는 하나도
  안 바뀌었다** — 대장이 넓어질 때마다 「보류(대상 아님)」 줄이 전부 거짓이 되는데도
  `LOOP.md § done_when` 첫 게이트는 초록불이었다. 그 게이트는 **「적혀 있나」만 묻고
  「적힌 게 아직 참인가」는 못 묻는다.** 파일은 둘째 질문에 답할 수 없고, 커맨드는
  기억하지 않아서 답한다. 남은 것은 커맨드 두 줄 — 습관으로 열어도 토큰을 안 태운다.
- **표준 라벨 세 축, `ns:value`.** `house:<repo>`(여럿) · `state:`(단일값, 6) · `ball:`(단일값, 3).
  문법은 `forge-config` 스위퍼 규약(`label-set`, 누적 금지)에서 물려받고 **어휘는 갈랐다** —
  거기 `agent:done` 은 「1차 검수 완료」라 같은 낱말이 두 뜻으로 굳는 것을 막았다.
  `ns:value` 문자열뿐이라 GitHub·GitLab·Forgejo 어디로 가도 산다.
- **라벨 없음 = 미분류 = 빚 전부.** 「재분류필요」라는 값이 통째로 사라졌다. 적을 게 없으니
  낡을 수도 없고, 빚 숫자가 **규율이 아니라 구조로** 정직해졌다.
- **`LOOP.md § never` 에서 라벨만 열렸다** (GLG). 금지의 이유(*"자동 답신은 남의 트리아지
  용량을 갉는다"*)는 **코멘트의 성질**이지 라벨의 성질이 아니다 — 라벨은 알림을 안 쏜다.
  코멘트와 닫기는 그대로 닫혀 있다.
- 라벨 정의 143개 생성, 이슈 36건 부착. 전체 빚 33 → 17.

### 판 — `./run.sh board`

- **캐시 없다.** 실측이 그렇게 시켰다: `gh search` 2.08s, GraphQL 한 방 1.45s(85건+라벨).
  느린 게 문제가 아니었으므로 사본을 만들 이유도 없다 — 사본은 기기마다 갈라진다.
- **`board --mine`** — 담당자가 자기 집에서는 결코 못 보는 것. **확정**(라벨/리포)과
  **후보**(남의 집 이슈가 내 리포 이름을 부른 것)를 갈라서 낸다. 후보는 판정이 아니라 제안이므로
  담당자가 가른다. 인자를 빼면 **cwd 의 git remote 로 자기 집을 유추**한다 — 이름을 타이핑해야
  하면 버튼이 아니라 질의문이다.
- **`done_when` 둘째 게이트가 유도로 돈다.** `wt=` 를 저장하지 않는다 — 경로는 판정이 아니라
  규약이고, 저장된 문자열은 사라진 워크트리와 짝이 맞을 수 있었다. 지금 세 방향을 본다:
  자리 없는 attempt(`entwurf#78`) · **receipt 없는 attempt**(`doomemacs-config#11` — 워크트리는
  있고 `NEXT--attempt-*` 가 없다) · **라벨 없는 워크트리**(`entwurf#110`).
  가운데 것이 2차 검수에서 나왔다 — 디렉터리 존재만 보면 게이트가 초록불인데 receipt 는 아직
  `git show 49ca747:TRIAGE.md` 에만 있었다. **셋 다 지금 열려 있다. 게이트가 도는 것이지
  판이 깨끗한 것이 아니다.**
- 단일값 위반은 **API 반환순 첫 값을 조용히 읽지 않고** `ambiguous` 로 보고한다.
  전이는 `./run.sh label --set <repo>#<n>=<state>[,<ball>]` — 같은 축의 기존 값을 지우고 하나만 쓴다.

### 낱말 하나가 두 뜻으로 굳었다 — 한 시간 만에

`state:proposed` 를 만들면서 LOOP 의 생애(분류→착수→검수→**제안**→머지)에서 화살표 이름을
그대로 상태 값으로 옮겼다. 그런데 **제안은 sorge 가 하는 행위**(「이 N개 머지합시다」)이고
이슈가 머무는 조건이 아니다. `agent-config` 담당자가 여섯 건에 `state:proposed` 를 붙였는데
그 읽기(「결정 대기 중인 제안」)가 조율 판에서는 자연스러운 쪽이고, 게이트는 그 여섯을
「워크트리 없는 attempt」로 몰아세웠다 — **게이트가 내 잘못을 남에게 물은 것이다.**

`proposed` 는 워크트리를 요구하지 않는다. 자리를 요구하는 것은 **착수와 검수 둘뿐**이다.
이 집이 자기 이름으로 부르는 병(*"같은 낱말이 두 뜻으로 굳는다"*)을, 그것을 막으려고 쓴
어휘 안에서 한 시간 뒤에 다시 잡았다.

### 계약 — 말이 시스템에 자리를 내줬다

- **`LOOP.md` 223 → 128줄.** 뺀 것 대부분은 **시스템이 없어서 말로 적어둔 절차**였다.
  네 조각이 그 일을 가져갔다 — **board**(판) · **heartbeat**(두드림) · **dm**(알림) ·
  **decision-gate**(GLG 결정). 각자 자기 집에서 자라므로 여기엔 *무엇을 대신하는지*만 적는다.
- **한 방향이 뒤집혔다.** *"루프가 알리지 않는다. GLG가 당긴다"* → `dm` 이 생기면서 **미는 쪽**이
  됐다. 다만 미는 것은 알림뿐이고, 승인·지시는 그 채널로 안 들어온다.
- **receipt 의 집이 정해졌다** — attempt 워크트리(`~/repos/wt/<house>/<이슈>/NEXT--attempt-*`),
  그 집 시민이 쓴다. 한동안 문서가 「이슈 코멘트에 산다」고 약속했는데 **코멘트는 금지되어 있었다.**
  교차검수가 잡았다.
- **문서를 줄이는 필터가 바뀌었다.** GLG: *"다 똑똑한 형제들이니까 규약 규칙으로 옥죌 필요 없다."*
  전엔 「스킬이 드나 / 이슈가 드나 / 정말 계약인가」였고, 이제 그 앞에 질문이 하나 더 있다 —
  **「읽는 형제가 유도할 수 있나」.** 빠진 것 대부분은 틀린 게 아니라 **참인데 불필요**했고,
  매번 읽는 파일에서는 그쪽이 더 나쁘다. `AGENTS.md` 12,356→11,625 · `SKILL.md` 9,678→7,484.
- 문서 대 코드가 **15:1 에서 대략 1:1** 이 됐다(09-08 실측 「문서 1,528줄 대 스킬 97줄」 대비).

### 교차검수 — 열 건, 두 형제

**terra**(`openai-codex/gpt-5.6-terra`): 주석이 계약을 재해석할 권한은 없다 → 논증을 스크립트에서
`LOOP.md` 로 옮기고 GLG 가 개정했다 · receipt 를 코멘트에 약속해놓고 코멘트는 금지인 자기모순 ·
필터를 건 뷰가 `open N` 이라고 거짓 집계 · add-only 는 단일값을 못 지킨다 · 이관 입력이 이관 자신에
의해 사라졌다(`--from-ref`).

**agent-config 담당자**(`claude-opus-5`): **배선이 아니라 목차가 빠졌다** — 심링크 사슬은 이미 6개
하네스까지 서 있었고 `SKILL.md` 에 그 물건 이름이 없었을 뿐이다 · 리포 이름은 단어다
(`edgeagent-config` 가 `agent-config` 로 잡혔다, 9→7) · **판정이 내려앉을 자리가 없었다** —
후보를 「내 몫」이라 정해도 그 집에 `house:` 어휘가 없으면 판정이 증발한다.

마지막 것이 설계 하나를 뒤집었다. `house:` 를 **「어느 리포가 이슈를 들고 있나」**로 게이팅했는데,
그 라벨이 답하는 질문은 **「이 일이 집을 가로지르나」**다. 둘은 갈라지고 어느 리포든 그런 이슈를
들 수 있다 — 이제 필요해지는 순간 지연 생성한다.

### 물린 주장

**「컨테이너용 빌드 산출물의 소유가 어느 집인가」는 횡단 발견이 아니었다.** `~/AGENTS.md:190-192`
가 이미 답하고(sibling-repo skill CLI 예외: 형제 리포에서 빌드 · gitignored 동봉 · `{baseDir}/<bin>`),
실물도 계약대로였다. **확인 없이 물려받아 이름 붙여 올렸고, 그 프레이밍이 다른 형제의 전제로
세탁됐다.** 사람이 아니라 물린 receipt 를 기록한다.


## v2026.9.8 — 루프가 한 밤을 돌았고, 이 집이 무엇이 아닌지 알게 됐다

`v2026.9.4` 가 집을 세웠다면 이번은 **그 집을 실제로 돌려본 나흘**이다. 세 가지가 닫혔다 —
이슈판이 계약을 얻었고, 첫 무인 밤이 실측으로 끝났고, 축 하나를 폐기했다.

### 루프가 계약을 얻었다

- **`LOOP.md` 가 섰다** (`644e62d`). 부르는 것은 sorge, 앉는 것은 그 집 시민, 미는 것은 GLG.
  GLG 가 2026-09-07 에 이전 계약(「자동 발신 금지 — 부르는 것은 GLG 의 손」)을 뒤집었다:
  *"내 손을 떠나야 한다."*
- **`TRIAGE.md` 원장 형식을 골랐다.** 8칸 표가 아니라 `key=value` 한 줄 — `grep sorge#5 TRIAGE.md`
  가 통하고, 첫 필드만 잘라 집합 연산이 되고, diff 가 이슈 단위로 난다.
- **두 절의 규율이 다르다**: 이슈 절은 *제자리 갱신되는 현재 판*, tick log 절은 *append-only 이력*.
  `done_when` 측정은 이슈 절의 첫 칼럼과 `wt=` 실재만 본다.
- **착수 격자 다섯 축**(권한·되돌리기·금전·신뢰·기한). 모델 자기 확신도는 안 쓴다 — 문헌이 기각했다.
  `trust=외부` 는 무조건 GLG 결정필요다. 발신자를 못 믿는데 자동 착수하면 그게 프롬프트 인젝션의 문이다.

### 첫 무인 밤 — 15 tick, 22:52→06:12 KST

GLG 지시(*"자는 동안 작업 좀 하게"*)로 opus worker 를 `/loop 30m` 시계에 걸었다.

- **미분류 73 → 0.** 총 83건이 전부 원장에 올랐다
- `done_when` 두 조건이 tick 3(23:26)에 충족됐고, **이후 13 tick 이 연속 `noop`**
- **착수 0 · 검수 0 · 제안 0** — `doomemacs-config#11` 이 WIP=1 을 잡고 있었고 그 defect 2건은 GLG 몫이었다
- 커밋 0 · 푸시 0 · 이슈 밖 쓰기 0 · 형제 호출 0 · entwurf 무접촉
- **가장 값진 산출은 원장이 아니라 판독이다** — worker 자신이 tick 15 에 적었다:
  *"감지기로서의 이 루프가 잡은 사건은 0건이었다."* 밤새 새 이슈 0, 닫힌 이슈 0, 시민 회신 0

측정으로 얻은 함정 둘:

- **로케일이 집합 연산을 조용히 틀리게 했다.** ko_KR 정렬로 만든 파일에 `comm` 이 「미분류 74」를
  냈고 거기 **이미 분류된 `sorge#1` 이 들어 있었다.** `LC_ALL=C` 로 다시 재서 73 을 얻었다.
  틀린 쪽이 「덜 분류됨」이 아니라 **「이미 한 것을 또 하기」** 였다
- **tick 당 5개 상한을 대상 밖 줄에도 걸면 13 tick(약 6.5시간)을 정보 없는 줄에 쓴다.**
  그래서 상한은 **대상 이슈 기준**으로 고쳤다 — 대장 밖 리포의 이슈는 판정이 없는 한 줄이라 안 센다

### Forgejo 축을 폐기했다

- GLG 판정 2026-09-08: *"forge.junghanacs.com은 일단 안 쓸 거야. 그냥 공개 github로 할거야.
  내 개인프로젝트 뭉치니까. forgejo는 대상에서 아예 빼자. 내가 생각하는 그림은 forgejo github
  gitlab 이런거랑 상관없다는 것을 알게 되었어."*
- **RAIL 10 이 「붙일 축」에서 「붙이지 않기로 한 판정」이 됐다.** `LOOP.md` 의 watch 는
  `gh search` 한 축만 본다. `LEDGER.md` 기술스택 선례에 판정 한 줄로 들어갔다.
- 「Forge 는 Forgejo 이슈를 못 가져온다」는 측정은 여전히 참이지만 **더는 이 집의 결함이 아니다** —
  「깃허브에 갇히지 않는다」가 *호스팅을 여럿 붙이는 문제* 라는 전제가 틀렸다.

### 이슈판 (RAIL 9) — 닫힘 표시만

RAIL 9 는 2026-09-04 에 끝났고 **그 실물 서술은 `v2026.9.4 § 이슈판` 이 이미 담았다.**
여기서는 RAIL 이 닫혔다는 사실만 든다 — 같은 사실을 닫힌 이력에 두 벌 적지 않는다.

### `sorge#1` — 세 집이 조건 다섯을 커밋으로 닫았다 (2026-09-06)

세 집이 각자 커밋으로 반환하고, 이 집은 본문에 정정·증거만 접었다. **코드는 그 집들이 고쳤다.**

| 조건 | 집 | 커밋 |
|---|---|---|
| 3 authority·ro 선언 | `nixos-config` | [`1f6c75f`](https://github.com/junghan0611/nixos-config/commit/1f6c75f) |
| 2a 진입점 absent 응답 | `agent-config` | [`ad347ef`](https://github.com/junghan0611/agent-config/commit/ad347ef) |
| 2b 불변식 · 1 B층 | `andenken` | [`1e61698`](https://github.com/junghan0611/andenken/commit/1e61698) |
| 7 양쪽 검증 | `nixos-config` | [`55ef65d`](https://github.com/junghan0611/nixos-config/commit/55ef65d) |
| 5 dirty·timeout | `nixos-config` | [`883cee2`](https://github.com/junghan0611/nixos-config/commit/883cee2) |

남은 넷(6 기억축 복구 · 4 MD freshness · 9 wrapper 은퇴 · 10 제공자 오류 원인)은 `NEXT.md` 가 든다.

**이 판의 방법론적 교훈 하나:** 하루 종일 **요약** 위에서 돌다가, 원문을 꺼내고서야 한가운데로 왔다 —
*"임베딩 제공자 오류로 현재 의미 검색이 비활성 상태입니다"*(gpt, 2026-09-03). 「소스로 브리핑하라」를
형제들에게는 매번 붙여 보내면서 이슈 자체는 요약 위에서 굴렸다.

### 대장

- **`sicm-study` 합류** (`3682c12`) — 이전 세입자의 이름을 그대로 둔 방에서
- `doomemacs-config` 판정 칸과 근거가 사흘간 어긋나 있던 것을 sol 검수가 잡아냈다

### 이관 — 이 집에 쌓인 남의 짐을 넘겼다

GLG 지시로 `sorge` 에 걸려 있던 entwurf 건 넷을 그 집이 가져갔다 (2026-09-08):

| 이 집 | 간 곳 |
|---|---|
| `sorge#8` turn 없는 퇴근 verb | `entwurf#106` |
| `sorge#9` 다른 기계의 형제에게 닿을 길이 없다 | `entwurf#107` |
| `sorge#10` placement 가 실 MCP wire 를 안 지난다 | `entwurf#108` |
| (새로 난 것) OpenClaw ↔ entwurf 다리 | **`entwurf#109`** |

`#109` 는 `#107` 에 얹지 않고 따로 섰다. 원인 계열은 같으나(dispatch 가 한 로컬 경로
네임스페이스를 전제한다) **수락조건이 다르다** — 마운트·MCP scope 대 인증 SSH 레일.
판정자는 entwurf 담당자다.

### 정정 — 넘긴 사실 하나가 측정으로 은퇴했다

sorge 가 **미검증 딱지를 붙여** 넘긴 줄: *"pi-shell-acp 의 `pi.extensions` 에 `entwurf-control.ts`
등 4개가 자동 load 된다"* (nixos-config 세션 2026-05-19 인용).

entwurf 담당자 재측정: **인용은 맞지만 그 선은 끊기지 않았다.** HEAD 0.19.0 `package.json:172-177`
이 여전히 `entwurf-control.ts` 를 싣는다. 끊긴 것은 `plugins/openclaw` — child **pi** 를 spawn 하던
쪽(`bf4a533`). 오늘 B 는 claude CLI 라 그 축을 안 탄다.

**결론은 같고 이유가 바뀌었다** — 잃어버린 선을 다시 잇는 일이 아니라, 이미 네 번 해본
MCP install adapter 의 다섯 번째다. 미검증 표시를 달고 넘겼기 때문에 은퇴가 사고 없이 일어났다.


## v2026.9.4 — 집이 서고, 첫 순회가 돌았다

하루 만에 빈 그릇에서 여덟 리포를 든 집이 됐다. 아래는 그날 닫힌 것들이다.

### 집을 세웠다

- **이름이 곧 계약이다.** 하이데거의 `Fürsorge` 두 갈래 중 *앞서 뛰어감* 을 택했다 —
  대신 해주지 않고, 앞서 가서 담당자에게 그의 몫을 돌려준다. 2026-08-10 의 9.1GB Zig 캐시
  사건이 그 갈림의 실례다.
- **`AGENTS.md`(계약) · `LEDGER.md`(판정 대장) · `NEXT.md`(다음 한 걸음)** 세 축을 세웠다.
- **순회 스킬 실물은 이 집에 산다** (`.claude/skills/sorge/`). `agent-config` 는 상대형
  심링크로 가리키기만 한다 — 실물은 하나, 나머지는 링크.
- 이 방의 제목을 담당자가 지었다: `§sorge #담당자 대신 해주지 않고 앞서 간다 —
  판정만 드는 대장과 계를 가로지르는 발견`. GLG 가 표시만 남기고 비워 둔 자리였다.

### 대장이 판정을 든다

- **「어느 노트가 이 리포의 담당자 문서인가」는 판정이지 유도 가능한 사실이 아니다.**
  대장이 denote id 로 든다. 제목의 `§`·`#담당자` 는 사람 눈을 위한 힌트일 뿐이다 —
  그것을 정의로 착각해 유도하려다 하루에 세 번 다르게 깨졌다.
- **대상 목록은 GLG 가 요청할 때만 늘어난다** (GLG 판정). `~/repos/gh` 는 대상이
  살 수 있는 장소이지 대상의 정의가 아니다. 그 한 줄로 순회에 올라오는 것이 **28 → 0**.
- **곁노트** 갈래가 생겼다 — 담당자 문서는 아니지만 유도되지 않는 판정(「이건 왜 층이다」,
  「이건 폐기됐으니 참고만」). `근거` 칸이 들고 `--brief` 가 통째로 실어 나른다.
- **빚은 급한 것이 아니다** (GLG 판정). 문제는 담당자 문서가 낡은 것이 아니라 **없는** 것이다.
  순회의 무게는 `문서 미정` 에 있지 `빚` 에 있지 않다.
- 판정 여덟: `sorge` `apply` `prime-agent` `forge-config` `junghan0611` `zotero-config`
  `denotecli`, 그리고 후보로 `doomemacs-config`.

### 첫 순회 — 담당자 아홉이 돌았다

- 담당자 다섯을 각자 리포에서 열어 담당자 문서를 갱신하게 했다. **빚 2 → 0.**
- **횡단 발견 레인을 열었다.** 사례 하나로는 안 열었다 — 다섯이 하루에 넷을 물어왔고
  셋이 서로 모르는 집에서 같은 모양으로 나왔다. 첫 줄의 처방은 `prime-agent` 담당자가
  정식화했다: **움직이는 것을 가리키지 말고 안 움직이는 것을 가리켜라.**
- 그날 잡힌 것들: 담당자 문서가 든 README 사본이 원문과 갈라짐 · 계약서가 승격돼 사라진
  파일을 세 곳에서 가리킴 · 「레인이 서 있다」와 「레인이 동작한다」의 차이 ·
  「아직」과 「이미」를 같은 칸에 적으면 3개월 뒤 구별되지 않음 · 생성물 위의 교정은
  하류가 아니라 상류에 · 독스트링은 코드의 사본이라 갈리면 사본이 이김.
- **순회가 자기 오탐에 걸렸다.** `#+hugo_lastmod` 에서 날짜만 읽고 시각을 버려서 도장 찍은
  날 그보다 이른 커밋을 빚으로 셌다. `zotero-config` 담당자가 잡았고 고쳤다.
- 담당자 아홉이 GLG 의 롤링 페이퍼 원탁(`20260408T120252`)에 앉았다.

### 이슈판

- **`datasette/metadata.yml`** — Magit Forge 의 로컬 캐시 위에 얹은 렌즈 넷:
  횡단 이슈 · 열린 판 · 리포별 집계 · 오래 열린 것. **DB 는 만들지 않았다** — 읽기만 하고,
  소유는 `doomemacs-config` 다.
- **`--immutable` 은 쓰지 않는다.** 그 플래그는 파일이 안 바뀐다는 전제로 락과 변경 감지를
  끄고 행 수를 시작 시점에 굳히는데, 그 파일은 GLG 의 Emacs 가 `forge-pull` 할 때마다
  바뀐다 (SQLite 문서 · datasette#1870).
- **Forgejo 이슈는 이 캐시에 구조적으로 안 들어온다** — `forge-forgejo.el:29` 가
  `forge-unusedapi-repository` 를 상속하고 그 `forge--pull` 은 `git fetch` 뿐이다.
  그 축은 REST 로 따로 본다.
- **「포지」가 세 물건을 가리킨다** — Forgejo(서버, `forge` 스킬이 쓴다) · Magit Forge
  (Emacs 클라이언트가 GitHub 에서 긁은 캐시, 읽는다) · 그 위의 렌즈. 이름은 안 바꾸고
  한정어 한 겹을 화면에 달았다.
- **`run.sh`** — 순회·이슈판·대장의 손잡이. SSOT 가 아니다.

### 도구 쪽으로 나간 것

- **`forge` 스킬 실물이 `agent-config` 에서 `forge-config` 로 옮겼다.** 도달 6/6 실측.
  근거는 커밋 짝이었다 — `SKILL.md` 와 `bin/forge` 가 다섯 날짜 전부 1:1 로 함께 커밋됐다.
  이제 sibling 심링크가 넷이다: `voscli` · `incidentcli` · `sorge` · `forge`.
- **`denotecli` 가 `#+hugo_lastmod` 와 abstract 를 싣게 됐다.** 그래서 `sweep.py` 가
  org 파일을 직접 열어 정규식으로 도장을 파던 **병렬 파서를 버렸다** — 바꾸기 전 8/8 일치 확인.
  아침에 남의 집에서 이름 붙인 고장을 저녁에 자기 몸에서 걷어낸 셈이다.
- **Claude Code 는 `CLAUDE.md` 만 자동 발견하고 리포 `AGENTS.md` 를 안 읽는다.**
  `agent-config` 담당자의 발견을 받아 재확인하고, GLG 지시로 **15개 리포에 11바이트씩**
  (`@AGENTS.md`) 넣었다. 이 집이 1인칭 증거였다 — 아침에 GLG 가 「먼저 읽어라」고
  말해줘야 자기 계약을 읽었다.

### 계약에 박힌 것

- **파생 가능한 것은 저장하지 않는다. 그러나 판정은 저장한다.** 그 갈림이 이 집의 규율이다.
- **「없다」는 결함이 아니다.** 아직 판정되지 않았을 뿐이고, 「관리 안 함」도 답이다.
- **자동 발신 금지.** 순회는 보고까지다. 형제를 부르는 것은 GLG 의 손이다.
- **조율자 경유 릴레이는 푸시 게이트를 열지 못한다.** 같은 지시를 받은 다섯이 3:2 로 갈렸고
  멈춘 쪽이 문언상 맞았다. GLG 는 조항을 고치는 대신 **경로**를 썼다 — 세션마다 직접 한 줄.
