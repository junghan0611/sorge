# CHANGELOG — sorge

닫힌 것만 여기 산다. 앞으로의 축은 `NEXT.md` 가 든다.

## Unreleased

## v2026.9.4 — 집이 서고, 첫 순회가 돌았다

하루 만에 빈 그릇에서 여덟 리포를 든 집이 됐다. 아래는 그날 닫힌 것들이다.

### 집을 세웠다

- **이름이 곧 계약이다.** 하이데거의 `Fürsorge` 두 갈래 중 *앞서 뛰어감* 을 택했다 —
  대신 해주지 않고, 앞서 가서 담당자에게 그의 몫을 돌려준다. 2026-08-10 의 9.1GB Zig 캐시
  사건이 그 갈림의 실례다.
- **`AGENTS.md`(계약) · `LEDGER.md`(판정 대장) · `NEXT.md`(다음 한 걸음)** 세 축을 세웠다.
- **순회 스킬 실물은 이 집에 산다** (`.claude/skills/sorge/`). `agent-config` 는 상대형
  심링크로 가리키기만 한다 — 실물은 하나, 나머지는 링크.
- 이 방의 제목을 담당자가 지었다: `§sorge #담당자 대신 해주지 않고 앞서 간다 —
  판정만 드는 대장과 계를 가로지르는 발견`. GLG 가 표시만 남기고 비워 둔 자리였다.

### 대장이 판정을 든다

- **「어느 노트가 이 리포의 담당자 문서인가」는 판정이지 유도 가능한 사실이 아니다.**
  대장이 denote id 로 든다. 제목의 `§`·`#담당자` 는 사람 눈을 위한 힌트일 뿐이다 —
  그것을 정의로 착각해 유도하려다 하루에 세 번 다르게 깨졌다.
- **대상 목록은 GLG 가 요청할 때만 늘어난다** (GLG 판정). `~/repos/gh` 는 대상이
  살 수 있는 장소이지 대상의 정의가 아니다. 그 한 줄로 순회에 올라오는 것이 **28 → 0**.
- **곁노트** 갈래가 생겼다 — 담당자 문서는 아니지만 유도되지 않는 판정(「이건 왜 층이다」,
  「이건 폐기됐으니 참고만」). `근거` 칸이 들고 `--brief` 가 통째로 실어 나른다.
- **빚은 급한 것이 아니다** (GLG 판정). 문제는 담당자 문서가 낡은 것이 아니라 **없는** 것이다.
  순회의 무게는 `문서 미정` 에 있지 `빚` 에 있지 않다.
- 판정 여덟: `sorge` `apply` `prime-agent` `forge-config` `junghan0611` `zotero-config`
  `denotecli`, 그리고 후보로 `doomemacs-config`.

### 첫 순회 — 담당자 아홉이 돌았다

- 담당자 다섯을 각자 리포에서 열어 담당자 문서를 갱신하게 했다. **빚 2 → 0.**
- **횡단 발견 레인을 열었다.** 사례 하나로는 안 열었다 — 다섯이 하루에 넷을 물어왔고
  셋이 서로 모르는 집에서 같은 모양으로 나왔다. 첫 줄의 처방은 `prime-agent` 담당자가
  정식화했다: **움직이는 것을 가리키지 말고 안 움직이는 것을 가리켜라.**
- 그날 잡힌 것들: 담당자 문서가 든 README 사본이 원문과 갈라짐 · 계약서가 승격돼 사라진
  파일을 세 곳에서 가리킴 · 「레인이 서 있다」와 「레인이 동작한다」의 차이 ·
  「아직」과 「이미」를 같은 칸에 적으면 3개월 뒤 구별되지 않음 · 생성물 위의 교정은
  하류가 아니라 상류에 · 독스트링은 코드의 사본이라 갈리면 사본이 이김.
- **순회가 자기 오탐에 걸렸다.** `#+hugo_lastmod` 에서 날짜만 읽고 시각을 버려서 도장 찍은
  날 그보다 이른 커밋을 빚으로 셌다. `zotero-config` 담당자가 잡았고 고쳤다.
- 담당자 아홉이 GLG 의 롤링 페이퍼 원탁(`20260408T120252`)에 앉았다.

### 이슈판

- **`datasette/metadata.yml`** — Magit Forge 의 로컬 캐시 위에 얹은 렌즈 넷:
  횡단 이슈 · 열린 판 · 리포별 집계 · 오래 열린 것. **DB 는 만들지 않았다** — 읽기만 하고,
  소유는 `doomemacs-config` 다.
- **`--immutable` 은 쓰지 않는다.** 그 플래그는 파일이 안 바뀐다는 전제로 락과 변경 감지를
  끄고 행 수를 시작 시점에 굳히는데, 그 파일은 GLG 의 Emacs 가 `forge-pull` 할 때마다
  바뀐다 (SQLite 문서 · datasette#1870).
- **Forgejo 이슈는 이 캐시에 구조적으로 안 들어온다** — `forge-forgejo.el:29` 가
  `forge-unusedapi-repository` 를 상속하고 그 `forge--pull` 은 `git fetch` 뿐이다.
  그 축은 REST 로 따로 본다.
- **「포지」가 세 물건을 가리킨다** — Forgejo(서버, `forge` 스킬이 쓴다) · Magit Forge
  (Emacs 클라이언트가 GitHub 에서 긁은 캐시, 읽는다) · 그 위의 렌즈. 이름은 안 바꾸고
  한정어 한 겹을 화면에 달았다.
- **`run.sh`** — 순회·이슈판·대장의 손잡이. SSOT 가 아니다.

### 도구 쪽으로 나간 것

- **`forge` 스킬 실물이 `agent-config` 에서 `forge-config` 로 옮겼다.** 도달 6/6 실측.
  근거는 커밋 짝이었다 — `SKILL.md` 와 `bin/forge` 가 다섯 날짜 전부 1:1 로 함께 커밋됐다.
  이제 sibling 심링크가 넷이다: `voscli` · `incidentcli` · `sorge` · `forge`.
- **`denotecli` 가 `#+hugo_lastmod` 와 abstract 를 싣게 됐다.** 그래서 `sweep.py` 가
  org 파일을 직접 열어 정규식으로 도장을 파던 **병렬 파서를 버렸다** — 바꾸기 전 8/8 일치 확인.
  아침에 남의 집에서 이름 붙인 고장을 저녁에 자기 몸에서 걷어낸 셈이다.
- **Claude Code 는 `CLAUDE.md` 만 자동 발견하고 리포 `AGENTS.md` 를 안 읽는다.**
  `agent-config` 담당자의 발견을 받아 재확인하고, GLG 지시로 **15개 리포에 11바이트씩**
  (`@AGENTS.md`) 넣었다. 이 집이 1인칭 증거였다 — 아침에 GLG 가 「먼저 읽어라」고
  말해줘야 자기 계약을 읽었다.

### 계약에 박힌 것

- **파생 가능한 것은 저장하지 않는다. 그러나 판정은 저장한다.** 그 갈림이 이 집의 규율이다.
- **「없다」는 결함이 아니다.** 아직 판정되지 않았을 뿐이고, 「관리 안 함」도 답이다.
- **자동 발신 금지.** 순회는 보고까지다. 형제를 부르는 것은 GLG 의 손이다.
- **조율자 경유 릴레이는 푸시 게이트를 열지 못한다.** 같은 지시를 받은 다섯이 3:2 로 갈렸고
  멈춘 쪽이 문언상 맞았다. GLG 는 조항을 고치는 대신 **경로**를 썼다 — 세션마다 직접 한 줄.
