# LEDGER — 판정 대장

> **여기 적히는 것은 사람의 판정뿐이다.** 유도 가능한 사실은 절대 적지 않는다 —
> 노트 유무, 커밋 수, 스킬 유무는 매 순회마다 다시 유도한다. 이유는
> `AGENTS.md § 대장의 유일한 규율`.
>
> 그래서 이 파일은 작다. 커지고 있다면 유도 가능한 것이 새어 들어온 것이다.

## 읽는 법

| 판정 | 뜻 | 다음 순회에서 |
|---|---|---|
| `배정` | 이 리포는 돌봄 대상이다. denote id가 있으면 그것이 담당자 문서 **정본** | 그 노트 기준으로 빚만 센다 |
| `관리 안 함` | 돌보지 않는다. 잠시 지나가는 작업이라 봇로그 올릴 일이 아니다 | **영영 뜨지 않는다** |
| `보류` | 필요하지만 지금은 아니다. 조건이 붙는다 | 조건이 풀리면 다시 뜬다 |
| `불필요` | 이 리포에는 그것이 없는 게 맞다 (수선 스킬·선례 표에서 쓴다) | 뜨지 않는다 |

**곁노트**는 담당자 문서가 아니면서 그 리포를 읽는 데 필요한 판정이다 — 「이건 **왜** 층이다」,
「이건 **폐기**됐으니 참고만」. 유도되지 않으므로 `근거` 칸에 id로 든다. 받는 담당자가 되물어야
할 것이므로 브리핑이 통째로 실어 나른다.

> **규약: `곁노트:` 는 `근거` 칸의 마지막 절로 쓴다.** 순회가 그 말부터 칸 끝까지를 잘라
> 브리핑에 붙이기 때문이다. 뒤에 다른 문장을 이으면 그것까지 딸려 나가고, 앞의 마침표에서
> 끊게 만들면 「폐기 문서다. 참고만 하고」의 뒷동강이 잘려 나간다 — 실제로 한 번 그랬다.

**표에 줄이 없는 것이 곧 「대상 아님」이다.** 그래서 `관리 안 함`은 이제 드물게만 쓴다 —
한때 대상이었다가 내려온 리포처럼, 없음만으로는 설명이 안 되는 자리에만.

어느 것도 영구가 아니다. 리포의 성격이 바뀌면 GLG가 다시 연다.

## 담당자 문서 — 리포 → denote id

**이 표가 대장의 심장이다.** "어느 노트가 이 리포의 담당자 문서인가"는 **GLG의 판정**이지
유도 가능한 사실이 아니다. 제목의 `§<repo>`와 `#담당자` 표시는 사람 눈을 위해 GLG가 남기는
힌트이지 정의가 아니다. 그것을 정의로 착각해 유도하려다 2026-09-04 하루에 세 번 다르게
깨졌다 — export 지연, 주제 노트 오인, 도장과 절차 어긋남. 셋 다 **판정을 유도로 착각한**
하나의 오답이었다.

저장할 것 중 denote id가 가장 안 썩는다. 같은 날 실측: 담당자 문서가 개명되면서 제목·슬러그·
태그·파일명이 전부 바뀌었고 `#+identifier`만 안 움직였다. 매번 유도하던 것(제목)은 하루에
한 번 바뀌었고, 저장하자는 것(id)은 Denote 설계상 영구다.

**순회의 서치는 버리지 않았다. 역할이 바뀌었다 — 서치는 후보를 제안하고, 이 표가 결정한다.**

| 리포 | 판정 | 담당자 문서 | 날짜 | 근거 |
|---|---|---|---|---|
| sorge | 배정 | `20260227T031800` | 2026-09-04 | GLG 지정. 「어떻게」 노트가 이 집의 담당자 문서가 됐다 — 만드는 법을 적은 방이 만드는 자의 방이 된 셈 |
| apply | 배정 | `20260331T172313` | 2026-09-04 | GLG 지정 — 「§apply #담당자 힣봇이 힣을 추천한다 — 그를 만나라」. 실을 것과 뺄 것은 그 집 담당자가 먼저 가른다 — 구직 기록이라 리포가 public이어도 내용은 가려야 한다 — **public 전환은 2026-08-27**(`apply/NEXT.md:24` + `gh repo view` createdAt, 2026-09-04 실측). 앞 세션이 영수증 없이 2026-09-04로 넘긴 것을 그 집 담당자가 잡아냈다. 곁노트: `20230725T102900`(`notes/`)이 이 리포의 **왜** 층 어쏠로그다 — 담당자 문서와 합치지 않고 이웃으로 둔다 (GLG 지정) |
| prime-agent | 배정 | `20260521T134542` | 2026-09-04 | GLG 지정 — 「§prime-agent #담당자 에이전트 오케스트레이터 리서치」. 곁노트: `20260827T222209`(`llmlog/`)는 **폐기 문서다 — 참고만 하고 정본으로 읽지 않는다.** 리포가 그 뒤로 진행됐기 때문이다 (GLG, 2026-09-04) |
| forge-config | 배정 | `20260527T073823` | 2026-09-04 | GLG 지정. id는 순회가 든 단독 후보를 그대로 실었다 (제목이 리포를 명시) |
| junghan0611 | 배정 | `20260318T183247` | 2026-09-04 | GLG 지정. 단독 후보 — 깃허브 프로파일 이력서 |
| zotero-config | 배정 | `20260304T105300` | 2026-09-04 | GLG 지정. 단독 후보 — `◊bibcli` 캡처 금고 |

**이 표에 없는 리포는 미판정이 아니라 대상이 아니다.** 2026-09-04 GLG: *"다 관심둘거
없어 내가 요청하면 대상에 넣으면돼."* 그러니 표가 짧은 것은 채워야 할 빈칸이 아니라
경계 그 자체다 — 근거는 `AGENTS.md § 대상`. 순회는 대상 밖 리포를 세지도 묻지도 않는다.

## 자기수선 스킬 (`.claude/skills/`)

| 리포 | 판정 | 날짜 | 근거 |
|---|---|---|---|
| — | — | — | 아직 판정 없음 |

## 횡단 발견 — 한 집에서 났고 여러 집에 걸린다

**이것이 이 집의 첫째 갈래다**(`AGENTS.md § 이 집이 있는 이유`). 발견은 들어올 때 계 전체를
가로질러 왔으니 나갈 때도 가로질러야 한다. 여기 적히는 것은 **어느 집에 걸리는가의 판정**이지
발견의 서술이 아니다 — 서술은 연대기(담당자 문서)가 든다.

레인을 2026-09-04에 열었다. 사례 하나로는 열지 않았다. **다섯 담당자가 하루에 넷을 물어왔고,
그중 셋은 서로 모르는 집에서 같은 모양으로 나왔다.**

| 발견 | 어디서 났나 | 어디에 걸리나 | 날짜 |
|---|---|---|---|
| **움직이는 것을 가리키지 말고 안 움직이는 것을 가리켜라.** 포인터는 사라진 이름을 가리키고, 사본은 원문과 갈라진다 — 둘 다 「가리킨 것이 움직였다」의 한 형태다 | `prime-agent`(승격된 `NEXT--<branch>.md` 를 세 곳에서 가리킴) · `junghan0611`(담당자 문서가 든 README 사본이 선언문 첫 문단에서 어긋남) | **담당자 문서를 쓰는 모든 집.** 특히 노트가 원문을 복제해 드는 곳 | 2026-09-04 |
| **조율자 경유 릴레이는 푸시 게이트를 열지 못한다.** 같은 지시를 받은 다섯이 3:2로 갈렸다 | 오늘의 순회 전체. `junghan0611`·`apply` 가 멈췄고 `forge-config`·`zotero-config`·`prime-agent` 가 진행했다 | **모든 조율 레인.** 근거는 `agent-config/home/AGENTS.md:106` — *"This routing rule … does not itself grant implementation, commit, or push approval."* **멈춘 쪽이 문언상 맞다.** GLG 판정 필요 | 2026-09-04 |
| **`agent-denote-add-heading` 은 TAG와 배치를 동시에 못 받는다.** `:LLMLOG:` 를 달면 파일 끝에 붙어 아카이브 절 뒤로 떨어진다 | `zotero-config` (docstring 실측) | **담당자 문서를 쓰는 모든 집** — 태그 붙은 보고를 넣을 때마다 손으로 옮겨야 한다. 고치는 손은 `agent-config`/`doomemacs-config` (남의 집) | 2026-09-04 |
| **계획을 현황도 자리에 그리면 같은 낱말이 두 뜻으로 굳는다.** `(앞으로 추가)` 딱지가 붙어 있어도 3개월 뒤엔 아무도 그 딱지를 안 읽는다 | `forge-config` (`thin pointer` 가 파일 수준/CLI 수준 두 뜻으로 굳음) | 구조 다이어그램을 든 모든 집. **`(앞으로 추가)` 딱지 붙은 그림은 냄새다** | 2026-09-04 |
| **한 표면에서 참인 것을 전 표면의 완료로 보고한다.** 「링크를 걸었다」와 「모든 하네스에 뜬다」는 다른 사건이다 | `agent-config`(아침 「이 기기 sorge 세팅 끝」이 Claude Code 면 기준으로만 참이었다 — 그 집 측정, 전달받음) · `forge-config`(선 적 없는 계획이 현황도 칸에 3개월) | **여러 표면에 나가는 모든 것.** 스킬은 지금 네 면이다 — `~/.claude/skills` · `~/.pi/agent/skills/pi-skills` · `~/.pi/agent/claude-plugin/skills` · `~/.codex/skills`. **2026-09-04 재측정: `sorge` 는 네 면 전부에 있다. 그 구멍은 닫혔다** | 2026-09-04 |
| **생성물 위에서 사람의 교정은 하류가 아니라 상류에 둔다.** 안 그러면 다음 rebuild가 되돌린다 | `zotero-config` — `.bib` 여덟 파일이 전부 생성물인데, 고치는 자리는 렌더 결과가 아니라 Zotero 항목이다(`scripts/gen-bibtex.py:183` — *"Existing citationKey on the Zotero item is sacred"*) | 생성물을 드는 모든 집. **사람이 이기는 것은 렌더러와 싸워서가 아니라 렌더러보다 상류에 서기 때문이다** | 2026-09-04 |
| **순회가 도장 날짜만 읽고 시각을 버렸다.** 도장 찍은 날 그보다 이른 커밋이 빚으로 잡힌다 | `zotero-config` (커밋 18:32 vs 도장 21:55, 빚 1로 오보) | `sorge` 자신. **고쳤다** (2026-09-04, 시각까지 읽는다) | 2026-09-04 |

첫 줄이 가장 무겁다. **sorge의 창립 규율이 한 층 밖에서 깨진 것이기 때문이다** — 대장은
「파생 가능한 것은 저장하지 않는다」로 서 있는데, 담당자 문서가 README 사본을 들면 노트 층에서
같은 죄를 짓는다. 저장한 사본은 관리 대상이 되고, 관리되지 않으면 갈라지고, **갈라진 쪽이 더
오래 남는다.** `junghan0611` 담당자가 300줄 두 벌을 다시 동기화하지 **않고** 「제목의 `(SSOT)` 는
역사다」를 판정으로 박은 것이 옳다.

그 줄의 처방은 `prime-agent` 담당자가 정식화했다(그의 해석, 2026-09-04). 처음엔 두 사례가 달라
보였는데 — 하나는 죽은 포인터, 하나는 갈라진 사본 — **한 규칙이 둘을 덮는다: 움직이는 것을
가리키지 마라.** `NEXT--<branch>.md` 는 승격되면 사라지는 이름이고 `NEXT.md` 는 안 움직인다.
노트 제목은 하루에도 움직이고 `#+identifier` 는 안 움직인다. 처방이 「사본을 동기화해라」였다면
관리 대상이 하나 더 생겼을 뿐이다.

`prime-agent` 가 그 줄이 자랄 방향도 하나 남겼다(그의 해석, 채택 보류). 「포인터가 낡는다」와
「검사가 낡는다」가 같은 뿌리라는 것 — 둘 다 *한 번 세워두면 계속 참일 것*이라는 가정에서 난다.
원탁에서 세 집이 같은 자리를 물어왔다: `apply` 의 fail-open 검사기(꾸며 적은 행을 건너뛰며 `ok`),
`dictcli` 의 `validate` 가 3,989 트리플을 0 errors 로 통과시킨 것, `prime-agent` 의 안 무는 green.
**셋 다 「검사가 있다」가 「검사되었다」로 읽힌 자리다.** 레인을 늘리지는 않는다 — 레인 하나가
관리 대상 하나이고, 그건 이 집의 규율이다. 방향만 남긴다.

그리고 그 규칙은 같은 날 세 집에서 **증명됐다.** 다섯 중 넷이 `:rename t` 로 노트를 개명했고,
매번 제목·슬러그·파일명이 전부 움직이고 `#+identifier` 만 남았다. `zotero-config` 는 자기
`AGENTS.md` 의 「현재 제목」 줄이 **쓴 지 20분 만에** 낡는 것을 겪고 그 자리에 이렇게 적었다 —
*"That line has already gone stale once. The id did not move. That is the whole point."*
규칙을 주장하는 문서가 자기 몸으로 그 규칙의 증거를 들었다.

## 기술스택 선례

| 주제 | 판정 | 날짜 | 근거 |
|---|---|---|---|
| Clojure 네이티브 | JVM이 아니라 GraalVM `native-image` | 2026-09-04 | GLG가 프로젝트마다 말로 알려주던 것. 선례 7개가 이미 리포 안에 있다 |

## `#담당자` 표시 — 사람이 읽는 표지이지 조회 수단이 아니다

GLG의 말(2026-09-04): *"담당자라고 적는건 나 또는 외부에서 모든 존재들이 보기 편하라고
한거야. 내가 담당자문서 뭔지 까먹어서."*

그러니 표시는 **가든에서 사람이 알아보라고 있는 것**이고, 순회가 담당자 문서를 찾는 수단이
아니다. 찾는 일은 위 표가 한다. 2026-09-04 실측으로 `§`노트 23개 중 표시된 것은 6개인데,
**미표시 17개는 이제 결함이 아니다** — 표시를 붙이는 일은 순회를 믿기 위한 전제에서
내려와, GLG가 가든 쪽에서 편할 때 하면 되는 일이 됐다.

표시 형식도 아직 두 모양이다: `§sorge #담당자` 하나, `§repo: #담당자 <자세>` 다섯.
붙일 때 형식도 같이 정하면 된다. 급하지 않다.

## 대상 밖은 물음이 아니다

전에 여기 *"나머지 50개는 아직 묻지 않았다"* 라고 적혀 있었다. 그때는 참이었고 지금은
아니다 — 2026-09-04 GLG가 기본값을 뒤집었다. **묻지 않은 리포가 아니라 대상이 아닌
리포다.** 순회가 대상 밖을 후보와 함께 올리면 그건 요청받지 않은 곳을 넓히는 것이고,
`AGENTS.md § 그래서 금지되는 것` 의 둘째 줄에 걸린다.

물을 것이 남는 자리는 하나다: **대상인데 담당자 문서 id가 아직 없는 리포.** 여기엔 순회가
후보를 들고 물어도 된다. 2026-09-04 현재 그런 리포는 없다 — 여섯 줄이 전부 id를 들었다.

그날 이 자리가 한 번 제 몫을 했다. 순회가 `apply` → `20260331T172313`, `prime-agent` →
`20260521T134542` 를 후보로 올렸고 GLG의 판정이 둘 다 그것이었다. **서치가 맞혔다는 게 요점이
아니라, 서치가 맞혀도 결정은 여전히 대장이 받아 적었다는 게 요점이다** — 몇 시간 전 같은
서치가 `후보 없음` 이라 답했고 그 사이 바뀐 것은 GLG가 제목을 고친 것뿐이었다. 제목은 하루에도
움직이고 id는 안 움직인다.
