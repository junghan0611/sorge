# LEDGER — 판정 대장

> **여기 적히는 것은 사람의 판정뿐이다.** 유도 가능한 사실은 절대 적지 않는다 —
> 노트 유무, 커밋 수, 스킬 유무는 매 순회마다 다시 유도한다. 이유는
> `AGENTS.md § 대장의 유일한 규율`.
>
> 그래서 이 파일은 작다. 커지고 있다면 유도 가능한 것이 새어 들어온 것이다.

## 읽는 법

| 판정 | 뜻 | 다음 순회에서 |
|---|---|---|
| `불필요` | 이 리포에는 그것이 없는 게 맞다 | 뜨지 않는다 |
| `보류` | 필요하지만 지금은 아니다. 조건이 붙는다 | 조건이 풀리면 다시 뜬다 |
| `배정` | 담당자에게 넘겼다 | 결과가 올 때까지 뜨지 않는다 |

`불필요`는 영구가 아니다. 리포의 성격이 바뀌면 GLG가 다시 연다.

## 담당자 문서 (§ botlog)

| 리포 | 판정 | 날짜 | 근거 |
|---|---|---|---|
| sorge | 배정 | 2026-09-04 | 정본 = `20260227T031800` (§sorge #담당자). GLG 지정. 「어떻게」 노트가 이 집의 담당자 문서가 됐다 — 만드는 법을 적은 방이 만드는 자의 방이 된 셈 |
| apply | 배정 | 2026-09-04 | GLG 지정, 돌봄 대상에 포함. 판정 당시 private이었고 같은 날 GLG가 public으로 전환했다(2026-09-04). 담당자 문서에 실을 것과 뺄 것은 여전히 그 집 담당자가 먼저 가른다 — 구직 기록이라 공개 리포여도 내용은 가려야 한다 |

## 자기수선 스킬 (`.claude/skills/`)

| 리포 | 판정 | 날짜 | 근거 |
|---|---|---|---|
| — | — | — | 아직 판정 없음 |

## 기술스택 선례

| 주제 | 판정 | 날짜 | 근거 |
|---|---|---|---|
| Clojure 네이티브 | JVM이 아니라 GraalVM `native-image` | 2026-09-04 | GLG가 프로젝트마다 말로 알려주던 것. 선례 7개가 이미 리포 안에 있다 |

## `#담당자` 표시 — 관용이 도입되는 중이다

제목의 `§<repo>`는 **그 리포에 대한 노트**라는 뜻이고, `#담당자`는 **그 리포의 담당자
문서**라는 뜻이다. 둘은 다른 사실이다. 2026-09-04 실측으로 `§`노트는 23개인데
`#담당자` 표시는 **6개**뿐이다 — 관용이 최근에 도입됐다.

그래서 순회는 둘을 갈라 보고한다. 미표시 23개를 결함으로 올리지 않는다. 표시할지는
GLG가 정한다.

**이 구분이 없으면 실제로 틀린다.** `§andenken §nixos-config §agent-config §dictcli
에이전트 기억층` 같은 주제 노트 하나가 네 리포의 담당자 문서로 잘못 잡혔다(2026-09-04
실측, 수정됨). 표시된 노트가 미표시 노트를 항상 이긴다.

## 아직 판정하지 않은 것

첫 순회 전이다. 지금 비어 있는 것은 결함이 아니라 **아직 아무도 판정하지 않았다**는
사실 그대로다. 순회가 후보를 올리고, 판정은 GLG가 한다.
