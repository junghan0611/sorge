# NEXT — sorge

> 다음 한 걸음. 중기 축은 아직 없다 — 그릇이라 모양이 자라는 중이다.

# RAIL

- [x] **1. 집을 세운다** — 이름·계약·대장·경계 (2026-09-04)
- [x] **2. 순회 스킬** — 실물 `.claude/skills/sorge/`, agent-config은 상대 심링크로 연결만 (2026-09-04)
- [x] **3. 첫 순회 실증** — 52리포 실측, 유도 오답 셋을 잡았다 (2026-09-04)
- [x] **4. sorge 자신의 담당자 문서** — `20260227T031800` 에 연대기 개시 (2026-09-04)
- [x] **5. 대장이 담당자 문서를 denote id로 든다** — 서치는 제안, 대장이 결정 (2026-09-04)
- [ ] **6. 대장 초기값** ← CURRENT: **GLG와 직접 앉아서** 52개 중 무엇이 돌봄 대상인지 한 줄씩 채운다

# NOW

- **Next:** `sweep.py` 의 `확인 요청 25`를 GLG 앞에 놓고 한 줄씩 판정받는다. 후보가 이미 붙어 나온다 —
  「이게 맞나 · 다른 노트인가 · 관리 안 하나?」 셋 중 하나면 된다. 후보가 갈리는 곳부터 물으면 빠르다:
  `memex-kb`(3) · `geworfen`(3) · `nixos-config`(2) · `doomemacs-config`(2) · `entwurf`(2) · `homeagent-config`(2) · `org`(2) · `agent-config`(2) · `andenken`(2) · `dictcli`(2).
  후보 0인 7개(`cote` `homepage` `jeohmunga` `naver-saiculture` `openclaw-config` `password-store` `prime-agent` `sicm-study`)는
  「새로 만들 일인가 · 관리 안 할 일인가」만 물으면 된다.
- **값은 내가 채우지 않는다.** 지금 대장이 두 줄뿐인 것이 정직한 상태다(GLG 지시 2026-09-04).
- **판정 기준:** 두 번째 순회에서 `확인 요청`이 줄면 대장이 쓰이고 있는 것이다.
  안 줄면 스킬이 아니라 대장 쪽을 본다.
- **Read:** `AGENTS.md`(계약 전부) · 담당자 문서 `20260227T031800`(연대기 + 「어떻게」) · 원석 `20240705T144627`(왜)
- **Do not touch:**
  - 남의 리포에 직접 커밋하지 않는다. 발견·명명·전달까지다.
  - 대장에 유도 가능한 사실을 적지 않는다. **판정은 적는다** — 그 갈림이 이 집의 규율이다.
  - `~/repos/work`는 순회 대상이 아니다 — 회사 일은 `cos`가 맡는다.
  - `agent-config`에 이 스킬의 사본을 두지 않는다. 실물은 하나, 나머지는 링크.
  - 가든 내보내기·`notes/content/`는 GLG의 손이다. 검증은 `~/org/botlog` 원본에서 한다.
  - `entwurf`는 순회 발신에서 뺀다 — 빚을 GLG가 이미 알고 직접 갱신할 것이다(2026-09-04).

# 열린 물음

- **`#담당자` 표시 형식이 두 모양이다.** `§sorge #담당자` 하나, `§repo: #담당자 <자세>` 다섯.
  표시는 사람이 읽는 표지이지 순회의 조회 수단이 아니므로 급하지 않다. 붙일 때 형식도 같이.
- **담당자 문서의 `#+description`·abstract가 아직 옛 방(수선 스킬 지침)을 말한다.** 헤딩 불변 규칙대로 두었다.
  방 이름과 카드가 어긋나 있다. 바꿀지는 GLG.
- **이슈 레인의 모양.** 순회가 리포 넘나드는 이슈 관계를 보게 될 텐데, 그것을 여기
  이슈로 옮기는가 아니면 브리핑에만 싣는가. 대장이 차고 나서 정한다.
- **순회 주기.** 지금은 GLG가 부를 때만이다. 주기를 만들면 그 주기가 관리 대상이 되므로,
  필요가 실제로 아플 때까지 미룬다.
- **루프로 도는 형태.** OpenClaw 봇으로 도는 판본이 가능해 보인다. 지금 그 워크스페이스
  안에 틀을 넣는 것은 GLG가 원치 않는다(2026-09-04) — 결정이 아니라 현재 입장이다.
