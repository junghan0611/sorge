# NEXT — sorge

> 다음 한 걸음. 중기 축은 아직 없다 — 그릇이라 모양이 자라는 중이다.
> 이 집은 **메타 리포**다. 여기 적히는 것은 남의 집 일의 **상태**이지 그 집의 할 일이 아니다.

# RAIL — 현재 좌표

- [x] **1. 집·계약·대장·첫 순회·이슈판·루프 계약·첫 무인 밤** — `CHANGELOG.md` `v2026.9.4` · `v2026.9.8`
- [x] **2. 모양이 정해졌다 — 크로스 이슈 매니징.** GLG 2026-09-08: *"sorge 는 단독 이슈보다
      크로스 이슈를 매니징해야 한다."* `sorge#13` 닫고 `sorge#14` 열고 대장 9→12행 (`ad16141`)
- [ ] **3. `sorge#14` 를 굴린다** ← CURRENT: **재분류 빚 22줄을 털고**, 코디네이터의
      브랜치 한 칸 검수 결과를 원장에 반영
- [ ] **4. 루프 착수 절차를 다시 판다** ← PAUSED: **GLG 판정 대기 — 일반 계약을 갈지,
      `entwurf` 한정 예외로 둘지.** `LOOP.md:49,91-100` · `AGENTS.md:72,151` 은 active attempt 를
      「실재하는 워크트리 + cwd=wt 인 시민」으로 정의하는데, 지금 도는 레인은 **브랜치**다
- [ ] **5. 스킬로 환원한다** — 어느 문장이 스킬로 갈 수 있는가 / 이슈가 들 수 있는가 / 정말 계약인가

현재 좌표: 2 완료 → **3 진행** → 4 보류 → 5 미착수

# NOW

- **Hot group:** `entwurf` — 사용자층 확보 (`sorge#14`)
- **Next:** (1) **먼저 `TRIAGE.md` 의 `재분류필요` 22줄을 tick 으로 분류한다** (5개씩) →
  (2) 코디네이터 `20260908T211235-485de8` 의 **브랜치 한 칸** 검수 결과를 받아 `entwurf#78` 줄에 싣는다
- **⚠ 하지 마라:** 그 검수가 끝나도 **`#78` 을 `제안`/머지로 올리지 않는다.** 브랜치
  `fix/78-realhome-flavor@d67aff9` 는 `#78` 의 **한 칸(First evidence)** 만 닫고, 나머지 다섯 칸은
  물리 호스트를 요구한다. **하위 브랜치 산출물과 이슈 생애 state 는 다른 축이다**
  (terra 검수 ⑥, 2026-09-08)
- **Blocker:** 없음. 공은 그 집에 있다
- **Read:** `AGENTS.md`(계약 전부) · `LOOP.md`(루프 계약) · `sorge#14` 코멘트 셋(오늘 축의 정본) ·
  담당자 문서 `20260227T031800`
- **Do not touch:**
  - 남의 리포에 커밋·이슈 쓰기. **발견·명명·전달까지다**
  - 대장에 유도 가능한 사실. **판정만 적는다**
  - `~/repos/work` · `cos` · Forgejo — 대상 밖
  - `agent-config` 에 스킬 사본 두기. 실물 하나, 나머지 링크
  - `~/org` 커밋. 노트는 고치되 커밋은 GLG와 org 레인

# ACTIVE

## entwurf — 사용자층 확보 (`sorge#14`) ← CURRENT

**GLG 좌표:** *"설치면이 늘어나야 이 entwurf 가 산다."* / *"**macOS·WSL2 등 지원이 안 될
이유가 없다 — pi·claude-code 가 이미 다 도니까.**"* 다만 `#78` 규율(*"claim only what physical
evidence proves"*)은 유지 — **「될 것」과 「됐다」는 다르다.**

| 레인 | 상태 |
|---|---|
| `entwurf#78` OS 확대 | **진행** — 코디네이터 `…211235-485de8`(fable), 구현 `…211458-5bbccd`(opus), 브랜치 `fix/78-realhome-flavor`, **워크트리 없음** |
| `entwurf#95` codex lane | 대기 — 코디네이터가 *"결정 대기가 섞인 칸"* 이라 두 번째로 미룸 |
| `entwurf#110` 이중 등록 | **크리티컬 아님** — `.pi/` 가 npm 패키지에 없다(`npm pack` 실측, 3자 독립 측정 일치). 개발 환경 결함이고 「브랜치로 작업」이 이미 비켜간다 |

- **GLG 손:** PR `#77` 답글 게시 여부 (초안 `~/repos/wt/entwurf-78-harvest/.attempt-78-drafts/pr-77-reply-DRAFT-DO-NOT-POST.md`) · `#110` 의 A/B 설계 결정
- **회수물:** `~/repos/wt/entwurf-78-harvest/` — 1차 attempt 정본 243줄 + 초안 셋. **워크트리는 아니다**

## sorge 자신 — 두 이름을 가른다

**`AGENTS.md` 에 「순회」와 「루프」를 갈라 적는다.** 한 이름이라 어느 쪽 얘긴지 모호한 문장이 여럿이다.

| | **순회** | **루프** |
|---|---|---|
| 무엇 | 담당자들에게 **물어서** 계를 가로지른다 | 이슈를 **분류해서** 원장을 채운다 |
| 산출 (2026-09-04 / 09-07밤) | 횡단 발견 15행 | 원장 83줄 · 횡단 발견 0행 |

**15:0 을 「루프가 횡단 발견을 못 낳는다」의 증거로 쓰지 않는다** — 표본이 각각 하나이고 과업이
다르며 tick 지침이 애초에 이슈만 시켰다. **순위의 근거는 `AGENTS.md` 자신의 선언**이다:
*"첫째가 목적이고 나머지는 그 목적이 드러나는 방식이다."* 15:0 은 진단이 아니라 **체온**이다.

**2026-09-08 이 그 진단을 한 번 뒤집었다** — 대장의 횡단 발견 표가 나흘 만에 두 행 늘었는데,
순회도 루프도 아닌 **담당자들이 서로 정정하는 자리**에서 났다. 셋째 경로가 있다.

## 빚 — 다음 tick 이 먼저 볼 것

- **`TRIAGE.md` 에 `verdict=재분류필요` 22줄.** 대장이 넓어지면서 「보류(대상 아님)」 판정이
  낡았다. `agent-config` 12 · `entwurf` 4 · `nixos-config` 3 · **`sorge#12`·`#6`·`#7`**
  (그 셋은 `sorge` 이슈지만 house 가 새로 대상이 된 집이라 같이 들어온다 — terra 검수 P0)
- **교훈:** 그 줄들은 `done_when` 첫 게이트(집합 차)를 **계속 통과한다.** 적혀 있으니까.
  **측정이 초록불인 채로 답이 틀려 있었다.** `LOOP.md § done_when` 에 「대장 변경 시 재분류」 축이
  없다 (`guarded`)

## `sorge#1` — 남은 넷

| 조건 | 집 | 상태 |
|---|---|---|
| **6 기억축 복구** | `nixos-config` | 열림 — **6봇 중 5봇이 자기 기억축에 못 닿는다**(glg 85.4s 대 게이트 15s). GLG: *"85초는 특성이 아니라 고장이다"* |
| 4 MD 축 freshness | `andenken` | 열림 |
| 9 wrapper 은퇴 | `agent-config` | 조건 둘 다 섬 · GLG 허가 대기 |
| 10 제공자 오류 원인 | 미정 | 열림 |

이 집 몫은 반환이 올 때 ① receipt 재확인 ② 본문에 접기 ③ 판정만 남기기. **코드는 그 집들이 고친다.**

## 그 밖에 열린 이슈

`#12`(nixos-config 운영면) · `#7`(tmux seat, agent-config 몫) · `#6`(subagents — entwurf 담당자가
준 새 재료 있음: 형제 셋 실측, `outputSchema` 없이도 전문이 온 관측) · `#5` · `#4` `#3` `#2`(garden)

# DORMANT

- [2026-09-08] **openclaw 다리** — `sorge#13` 닫힘. **철회이지 유예가 아니다.** 다리를 건너는
  대신 B 를 컨테이너 밖으로 꺼냈다. 살아 있는 것은 다리가 아니라 **시계** 하나이고, 되살아나면
  `#109` 의 마운트 목록이 아니라 **새 이슈**다. B: *"내가 필요하다고 말할 때 연다."*
- [2026-09-08] **자체 루프 하네스 조사** — omp·copilot·grok 셋 다 **트리거가 없다**. 시계를 가진
  스택은 OpenClaw 하나. `LOOP.md § driver` 계약은 이 조사로 안 흔들렸다

# 열린 물음

- **`#담당자` 표시 형식이 두 모양이다** (`§sorge #담당자` 하나, `§repo: #담당자 <자세>` 다섯).
  표시는 사람이 읽는 표지이지 조회 수단이 아니므로 급하지 않다
- **순회 주기.** 지금은 GLG가 부를 때만. **루프의 주기와 혼동하지 않는다.** 주기를 만들면
  그 주기가 관리 대상이 되므로 필요가 실제로 아플 때까지 미룬다
- **오라클과 리포 수가 다르다** (oracle 52 / thinkpad 71). 대상을 넓힐 때 후보가 기기마다
  다르게 보일 수 있다
