# NEXT — sorge

> 다음 한 걸음. 중기 축은 아직 없다 — 그릇이라 모양이 자라는 중이다.

# RAIL

- [x] **1. 집을 세운다** — 이름·계약·대장·경계 (2026-09-04)
- [x] **2. 순회 스킬** — 실물 `.claude/skills/sorge/`, agent-config은 상대 심링크로 연결만 (2026-09-04)
- [x] **3. 첫 순회 실증** — 52리포 실측, 오답 둘 잡음(export 읽음 · §언급을 담당자로 오인) `f17d214` (2026-09-04)
- [x] **4. sorge 자신의 담당자 문서** — `20260227T031800` 에 [2026-09-04] 현재 보고 헤딩으로 연대기 개시 (2026-09-04)
- [ ] **5. 두 번째 순회** ← CURRENT: `판정 필요` 8이 0으로 수렴하는지가 이 집이 일하는지의 신호

# NOW

- **Next:** GLG가 부르면 `sweep.py` → `판정 필요` 8곳(cote·homepage·jeohmunga·naver-saiculture·openclaw-config·password-store·prime-agent·sicm-study)을
  브리핑으로 올리고 판정은 대장에. 빚 상위 셋(entwurf 429 · notes 150 · garden2wikidocs 68)은 `--brief` 블록 그대로 형제에게.
- **판정 기준:** 두 번째 순회에서 `판정 필요` 줄이 0에 수렴하면 이 집이 일하고 있는 것이다.
  줄어들지 않으면 대장이 안 쓰이고 있다는 뜻이고, 그때는 스킬이 아니라 대장 쪽을 본다.
- **Read:** `AGENTS.md`(계약 전부) · 담당자 문서 `20260227T031800`(연대기 + 「어떻게」) · 원석 `20240705T144627`(왜)
- **Do not touch:**
  - 남의 리포에 직접 커밋하지 않는다. 발견·명명·전달까지다.
  - 대장에 유도 가능한 사실을 적지 않는다.
  - `~/repos/work`는 순회 대상이 아니다 — 회사 일은 `cos`가 맡는다.
  - `agent-config`에 이 스킬의 사본을 두지 않는다. 실물은 하나, 나머지는 링크.
  - 가든 내보내기·`notes/content/`는 GLG의 손이다. 검증은 `~/org/botlog` 원본에서 한다.

# 열린 물음

- **빚의 기준선이 `hugo_lastmod`인데 `agent-denote-add-history`는 그것을 올리지 않는다.** 담당자가 히스토리·헤딩만 덧대면
  노트는 갱신됐어도 sweep엔 빚 그대로다. 첫 담당자 문서 갱신(2026-09-04)에서 `set-front-matter :hugo_lastmod`를 따로 불러 맞췄다.
  기준선을 파일 mtime·git으로 바꿀지, SKILL.md의 갱신 절차에 lastmod 한 줄을 넣을지 — GLG 판정.
- **`판정 필요`의 "최근 활동"이 `sweep.py`에 `2026-08-01`로 박혀 있다.** 한 달 뒤엔 모든 리포가 최근이 된다. `--since` 옵션이나
  "마지막 커밋 N일 이내"로 바꿀지.
- **`LEDGER.md` apply 줄의 근거 "private 리포이므로"** — `gh repo view junghan0611/apply` 실측(2026-09-04)은 PUBLIC. 판정은 GLG 것이라
  손대지 않았다. 근거만 고칠지, 판정을 다시 볼지.
- **담당자 문서의 `#+description`·abstract가 아직 옛 방(수선 스킬 지침)을 말한다.** 헤딩은 안 건드린다는 규칙대로 두었다. 카드를 바꿀지는 GLG.
- **이슈 레인의 모양.** 순회가 리포 넘나드는 이슈 관계를 보게 될 텐데, 그것을 여기
  이슈로 옮기는가 아니면 브리핑에만 싣는가. 첫 순회 뒤에 정한다.
- **순회 주기.** 지금은 GLG가 부를 때만이다. 주기를 만들면 그 주기가 관리 대상이 되므로,
  필요가 실제로 아플 때까지 미룬다.
- **루프로 도는 형태.** OpenClaw 봇으로 도는 판본이 가능해 보인다. 지금 그 워크스페이스
  안에 틀을 넣는 것은 GLG가 원치 않는다(2026-09-04) — 결정이 아니라 현재 입장이다.
