# NEXT — sorge

> 다음 한 걸음. 중기 축은 아직 없다 — 그릇이라 모양이 자라는 중이다.

# RAIL

- [x] **1. 집을 세운다** — 이름·계약·대장·경계 (2026-09-04)
- [x] **2. 순회 스킬** — 실물 `.claude/skills/sorge/`, agent-config은 상대 심링크로 연결만 (2026-09-04)
- [x] **3. 첫 순회 실증** — 52리포 실측, 유도 오답 셋을 잡았다 (2026-09-04)
- [x] **4. sorge 자신의 담당자 문서** — `20260227T031800` 에 연대기 개시 (2026-09-04)
- [x] **5. 대장이 담당자 문서를 denote id로 든다** — 서치는 제안, 대장이 결정 (2026-09-04)
- [x] **6. 대장 초기값** — GLG가 기본값을 뒤집었다. 대장에 오른 것만 대상, 목록은 요청으로만 늘어난다. 판정 여섯 (2026-09-04)
- [ ] **7. 두 번째 순회** ← CURRENT: 빚 둘을 형제에게 건넨다 — `prime-agent` 77커밋 · `junghan0611` 58커밋. 대장 여섯 줄은 전부 id를 들었다 (2026-09-04)

# NOW

- **Next:** 빚이 선 곳은 둘이다. `sweep.py --repo <name> --brief` 블록을 **그대로** 형제에게 던진다 —
  요약하지 않고, 곁노트 줄도 빼지 않는다. 부르는 것은 GLG의 손이다.
  - `prime-agent` — `20260521T134542`, 도장 2026-08-27 이후 **77커밋**.
    곁노트가 붙어 나간다: `20260827T222209`(`llmlog/`)는 폐기 문서이니 참고만.
  - `junghan0611` — `20260318T183247`, 도장 2026-07-10 이후 **58커밋**.
- **대장이 여섯 줄로 찼고 `문서 미정`이 0이다.** `apply`(`20260331T172313`)와
  `prime-agent`(`20260521T134542`)를 GLG가 판정했다. 순회의 후보 제안과 판정이 처음으로 일치했는데,
  **그건 서치에 결정을 돌려줄 근거가 아니다** — 같은 서치가 몇 시간 전 `후보 없음`이었고
  그 사이 바뀐 것은 제목뿐이었다. 제목은 하루에도 움직이고 id는 안 움직인다.
- **경계가 뒤집혔다 (2026-09-04).** *"다 관심둘거 없어 내가 요청하면 대상에 넣으면돼."*
  대장에 줄이 없으면 미판정이 아니라 대상 밖이다. 근거는 `AGENTS.md § 대상`.
  같은 71리포에서 올라오는 것이 **28 → 3**. 줄어든 25는 해결된 게 아니라 애초에 물을 것이 아니었다.
- **판정 기준:** 볼 것은 둘뿐 — 대상 리포의 빚이 줄어드는가, 대장이 가리키는 id가 실재하는가.
  「몇 개가 미판정인가」는 더 이상 신호가 아니다.
- **Read:** `AGENTS.md`(계약 전부) · 담당자 문서 `20260227T031800`(연대기 + 「어떻게」) · 원석 `20240705T144627`(왜)
- **Do not touch:**
  - **대상 목록을 스스로 넓히지 않는다.** 후보를 들고 "이것도 대상 아닌가요"를 먼저 꺼내지 않는다.
    묻는 자리는 `문서 미정` 레인 하나다.
  - 남의 리포에 직접 커밋하지 않는다. 발견·명명·전달까지다.
  - 대장에 유도 가능한 사실을 적지 않는다. **판정은 적는다** — 그 갈림이 이 집의 규율이다.
  - **`곁노트:` 는 `근거` 칸의 마지막 절로 쓴다.** 순회가 그 말부터 칸 끝까지를 브리핑에 싣는다.
    뒤에 문장을 이으면 딸려 나가고, 앞에서 끊으면 「참고만 하고」가 잘려 나간다.
  - `~/repos/work`는 순회 대상이 아니다. `cos`도 아니다 — 위치가 `gh`일 뿐 성격은 비서실장이다.
  - `agent-config`에 이 스킬의 사본을 두지 않는다. 실물은 하나, 나머지는 링크.
  - 가든 내보내기·`notes/content/`는 GLG의 손이다. 검증은 `~/org/botlog` 원본에서 한다.

# 열린 물음

- **`#담당자` 표시 형식이 두 모양이다.** `§sorge #담당자` 하나, `§repo: #담당자 <자세>` 다섯.
  표시는 사람이 읽는 표지이지 순회의 조회 수단이 아니므로 급하지 않다. 붙일 때 형식도 같이.
- **이슈 레인의 모양.** 순회가 리포 넘나드는 이슈 관계를 보게 될 텐데, 그것을 여기
  이슈로 옮기는가 아니면 브리핑에만 싣는가. 대장이 차고 나서 정한다.
- **오라클과 리포 수가 다르다.** oracle 52 / thinkpad 71. 대장이 대상을 들게 된 지금은
  판정이 같으므로 순회 결과도 같지만, 대상을 넓힐 때 후보가 기기마다 다르게 보일 수는 있다.
- **순회 주기.** 지금은 GLG가 부를 때만이다. 주기를 만들면 그 주기가 관리 대상이 되므로,
  필요가 실제로 아플 때까지 미룬다.
- **루프로 도는 형태.** OpenClaw 봇으로 도는 판본이 가능해 보인다. 지금 그 워크스페이스
  안에 틀을 넣는 것은 GLG가 원치 않는다(2026-09-04) — 결정이 아니라 현재 입장이다.
